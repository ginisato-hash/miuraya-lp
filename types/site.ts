export type SiteInfo = {
  name: string;
  nameEn: string;
  postalCode: string;
  prefecture: string;
  addressLine: string;
  area: string;
  operator: string;
  contactEmail: string;
  roomCount: number;
  buildingDescription: string;
  planType: string;
};
